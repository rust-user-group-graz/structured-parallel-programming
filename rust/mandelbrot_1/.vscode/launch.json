{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch Mandelbrot",
            "program": "${workspaceFolder}/target/debug/mandelbrot_1",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopOnEntry": false
        }
    ]
}